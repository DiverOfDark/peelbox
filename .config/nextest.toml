[profile.default]
slow-timeout = { period = "300s", terminate-after = 3 }

[profile.ci.junit]
path = "junit.xml"

[test-groups]
serial-tests = { max-threads = 2 }

[[profile.default.overrides]]
filter = 'test(container_) | test(e2e) | test(llm_) | binary(container_buildkit) | binary(container_e2e) | binary(llm_e2e) | binary(llm_embedded) | binary(static_e2e)'
test-group = 'serial-tests'
