[profile.default]
slow-timeout = { period = "300s", terminate-after = 3 }

[profile.ci]
junit = { path = "junit.xml" }

[test-groups]
serial-tests = { max-threads = 1 }

[[profile.default.overrides]]
filter = 'test(container_) | test(e2e) | test(llm_)'
test-group = 'serial-tests'
