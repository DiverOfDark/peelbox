{
  "name": "ruby-with-node",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
