{
  "name": "monorepo-example",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0",
    "express": "^4.18.0",
    "@types/express": "^4.17.0"
  }
}
