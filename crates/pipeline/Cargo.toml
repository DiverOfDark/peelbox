[package]
name = "peelbox-pipeline"
version.workspace = true
edition.workspace = true

[dependencies]
peelbox-core = { path = "../core" }
peelbox-wolfi = { path = "../wolfi" }
peelbox-detect = { path = "../detect" }
anyhow = "1.0"
tracing = "0.1"
thiserror = "2.0"
strsim = "0.11"

[dev-dependencies]
tempfile = "3.24"
